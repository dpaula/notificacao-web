{
  "name": "notification-permission-requester",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "esbuild --servedir=. --serve=localhost:8080",
    "build": "node build.js",
    "start": "serve -s dist -l ${PORT:-8080}"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "serve": "^14.2.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
